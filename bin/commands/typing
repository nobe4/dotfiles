#!/usr/bin/env bash

[ "$*" == "-h" ] && cat "${0}" && exit 0

t=600 # 10 * 60
open "https://ranelpadon.github.io/ngram-type/"
countdown ${t}

open "https://www.keybr.com/"
countdown ${t}

open "https://monkeytype.com/"
countdown ${t}
