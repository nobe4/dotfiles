#!/usr/bin/env bash

set -e

command="$*"

script -q /dev/null bash -c "$command" \
	| tee >(
		awk '
			/https:\/\/github.com/ {
				url = $2
				sub(/\033.*/, "", url)
				print url
				system("")
			}
		' | xargs -r -d '\n' -n1 open
	)
