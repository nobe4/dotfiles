# Install Vim
brew install macvim --HEAD --override-system-vim --with-cscope

# Install vim-plug
curl -fLo ~/.vim/autoload/plug.vim --create-dirs https://raw.githubusercontent.com/junegunn/vim-plug/master/plug.vim

# Add the swapfile and undodir folder
mkdir -p ~/.vim/swapdir/
mkdir -p ~/.vim/undodir/
