#!/usr/bin/env bash

choices="$(
	find \
		"${HOME}/dev" \
		-mindepth 1 \
		-maxdepth 2 \
		-type d &&
	find \
		"${HOME}" \
		-mindepth 1 \
		-maxdepth 1 \
		-type d \
)"

cd "$(
	echo "${choices}" | fzf --select-1 --query "${*}"
)" || exit
